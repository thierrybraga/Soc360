# Open-Monitor v3.0 — PRODUCTION requirements
# Instalação produção:  pip install -r requirements.txt
# Instalação dev/teste:  pip install -r requirements-dev.txt
#
# Política de versionamento:
#  - Pins exatos (==) para builds reproduzíveis.
#  - Dependências transitivas sensíveis a segurança são pinadas explicitamente.
#  - Como esta é uma plataforma de gestão de vulnerabilidades, NÃO embarcamos
#    dependências com CVEs conhecidos quando há patch disponível.

# ==========================================
# WEB FRAMEWORK (Flask core stack)
# ==========================================
Flask==3.0.3                 # 3.0.0->3.0.3: correções de segurança
Werkzeug==3.0.6              # CVE-2024-34069 (debugger RCE), CVE-2024-49767
Jinja2==3.1.5                # CVE-2024-22195/34064/56326 (XSS/sandbox)
MarkupSafe==2.1.5
itsdangerous==2.2.0          # assinatura de sessão/CSRF — pin reproduzível
click==8.1.7
blinker==1.8.2

Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
WTForms==3.1.1

# ==========================================
# DATABASE / ORM
# ==========================================
SQLAlchemy==2.0.36           # patch-level dentro de 2.0.x (bugfixes)
alembic==1.13.3
psycopg2-binary==2.9.9       # driver PostgreSQL (carregado em runtime via DB URL)

# ==========================================
# CACHE / BROKER / ASYNC
# ==========================================
redis==5.0.8
celery==5.3.6

# ==========================================
# SECURITY / AUTH
# ==========================================
bcrypt==4.1.1
cryptography==43.0.1         # 41.0.7->43.0.1: CVE-2023-50782/2024-0727/26130
email-validator==2.1.0       # usado implicitamente por WTForms/marshmallow
python-dotenv==1.0.0
tacacs_plus==2.6

# ==========================================
# HTTP CLIENT (NVD/EUVD/MITRE fetchers)
# ==========================================
requests==2.32.3             # CVE-2024-35195 (verificação de certificado)
urllib3==2.2.3               # CVE-2024-37891, CVE-2023-45803
certifi==2024.8.30           # CVE-2023-37920 (CA raiz comprometida)
idna==3.10                   # CVE-2024-3651
charset-normalizer==3.4.0

# ==========================================
# SERIALIZATION / API
# ==========================================
marshmallow==3.20.1

# ==========================================
# REPORTS (PDF/DOCX)
# ==========================================
WeasyPrint==60.1
pydyf==0.10.0                # compat. obrigatória c/ WeasyPrint 60.1 (API quebrou em 0.11+)
Markdown==3.5.1
python-docx==1.1.2

# ==========================================
# AI PROVIDER
# ==========================================
openai==1.6.1                # mantido pinado — bump exige revisar app/services/core/openai_service.py
httpx==0.27.2                # dep transitiva de openai (httpx>=0.23,<1) — pinada p/ build reproduzível

# ==========================================
# WSGI SERVER
# ==========================================
gunicorn==22.0.0             # CVE-2024-1135 (HTTP request smuggling)

# ==========================================
# OBSERVABILITY / UTILS
# ==========================================
structlog==23.2.0            # usado em run.py
python-dateutil==2.8.2       # dep transitiva de celery — pinada p/ reprodutibilidade
pytz==2023.3                 # dep transitiva de celery — pinada p/ reprodutibilidade

# ==========================================
# OPCIONAL — S3 bulk import
# ==========================================
# boto3 é importado de forma preguiçosa em app/tasks/nvd.py (bulk_import_task)
# apenas quando a origem do arquivo é s3://. Descomente se for usar import via S3:
# boto3==1.34.162
# botocore==1.34.162
